package me.codyq.spikot.gui

// TODO: Components, store logic for individual slot
// TODO: Views for handling logic of rendering things to a GUI

/*

fun myComponent(): Component = {
    //
}

var clicked = false

val myView = view {
    +component {
        location = (x, y)
        item = item(Material.WHITE_STAINED_GLASS)
        onClick = {
            // Toggle clicked variable
            clicked = !clicked
        }
    }

    // Will only render if click is true
    if (clicked) {
        +myComponent {
            location = (x, y)
            item = item(Material.GREEN_STAINED_GLASS)
        }
    }
}

val myGui = gui {
    view = myView

    autoRefresh {
        ticks = 1
        onClick = true
    }
}
 */